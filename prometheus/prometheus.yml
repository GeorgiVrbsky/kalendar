global:
  scrape_interval: 10s # Collect metrics every 10 seconds

scrape_configs:
  - job_name: 'spring-boot-dashboard'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080']
